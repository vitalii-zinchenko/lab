# Project

This is the experimentation project but the code should be production grade.

# Service Structure

When creating a new backend service, follow the architecture described in [services/be-service-template.md](services/be-service-template.md).

# Git

When commiting you must add files in .claude folder as well

# Debugging 

To get the logs for a service use the `tilt` cli.